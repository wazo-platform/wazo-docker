debug: true
rest_api:
  listen: 0.0.0.0

db_uri: postgresql://wazo:secret@postgres/wazo

auth:
  host: auth
  port: 9497
  prefix: null

bus:
  host: rabbitmq

celery:
  broker: amqp://guest:guest@rabbitmq:5672

service_discovery:
  enabled: false
